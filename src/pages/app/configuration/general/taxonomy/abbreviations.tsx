import MyGrid from "~/components/common/MyGrid";

export default function Abbreviations() {
  return (
    <MyGrid
      title="Abbreviations"
      rowData={[]}
      colDefs={[]}
      multiSelect={false}
      noToolbar
    />
  );
} 