import MyGrid from "~/components/common/MyGrid";

export default function CompanyNouns() {
  return (
    <MyGrid
      title="Company Nouns"
      rowData={[]}
      colDefs={[]}
      multiSelect={false}
      noToolbar
    />
  );
} 